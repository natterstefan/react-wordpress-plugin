import { defineMessages } from 'react-intl'

export default defineMessages({
  'react.settings.title': {
    id: 'react.settings.title',
    description: 'The title of the wp-admin plugin page',
    defaultMessage: 'PLUGINNAME Settings',
  },
})
